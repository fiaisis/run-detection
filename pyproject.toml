[project]
name = "run-detection"
readme = "README.md"
version = "0.0.1"


[project.optional-dependencies]
code-inpsection = [
    "toml==0.10.2",
    "yapf==0.32.0",
    "pylint==2.15.7"
]

ci = [
    "run-detection[code-inspection]",
]

[tool.yapf]
based_on_style = "pep8"
column_limit = 120